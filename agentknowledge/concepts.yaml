# AGENT: Component and design concept registry for the enterprise Bootstrap 5 theme.
# Each entry maps a concept name to its canonical file and related concepts.
# Agents should read this file at session start to orient themselves.

- name: "EnterpriseTheme"
  definition: "Bootstrap 5 variable overrides for compact, square, enterprise styling with reduced spacing and zero border radius."
  anchor_file: "src/scss/_variables.scss"
  related: ["CustomComponents", "OpenSansFont", "JetBrainsMonoFont", "EnterpriseThemeCommented"]

- name: "EnterpriseThemeCommented"
  definition: "Beginner-friendly version of _variables.scss with extensive tutorial-style comments for new developers."
  anchor_file: "src/scss/_variables-commented.scss"
  related: ["EnterpriseTheme"]

- name: "OpenSansFont"
  definition: "Open Sans loaded from Google Fonts as the primary body text font for screen readability."
  anchor_file: "src/scss/_variables.scss"
  related: ["EnterpriseTheme", "JetBrainsMonoFont"]

- name: "JetBrainsMonoFont"
  definition: "JetBrains Mono loaded from Google Fonts for fixed-width text with clear character distinction."
  anchor_file: "src/scss/_variables.scss"
  related: ["EnterpriseTheme", "OpenSansFont"]

- name: "CustomComponents"
  definition: "Custom enterprise SCSS component styles built on top of Bootstrap 5 — tables, cards, sidebars, toolbars, metrics, badges."
  anchor_file: "src/scss/custom.scss"
  related: ["EnterpriseTheme", "MetricCard", "Toolbar", "Sidebar", "BadgeStatus", "TableEnterprise"]

- name: "MetricCard"
  definition: "Dashboard card for displaying key metrics with labels, values, and change indicators."
  anchor_file: "src/scss/custom.scss"
  related: ["CustomComponents", "EnterpriseTheme"]

- name: "Toolbar"
  definition: "Vanilla TypeScript programmable action bar with regions, split buttons, gallery controls, docked/floating modes, drag-to-dock, Priority+ overflow, KeyTips, layout persistence, resize, and WAI-ARIA toolbar pattern. Sets --toolbar-<position>-size CSS custom properties on <html>."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["CustomComponents", "ToolbarStyles", "EnterpriseTheme", "BootstrapIcons", "SidebarPanel", "StatusBar", "BannerBar"]

- name: "ToolbarStyles"
  definition: "SCSS styles for the Toolbar component with toolbar- prefixed classes, docked/floating/orientation modifiers, split button dropdowns, gallery popups, overflow menu, KeyTip badges, dock zone indicators, and resize handle."
  anchor_file: "components/toolbar/toolbar.scss"
  related: ["Toolbar", "EnterpriseTheme"]

- name: "Sidebar"
  definition: "Dark sidebar navigation component with active state highlighting."
  anchor_file: "src/scss/custom.scss"
  related: ["CustomComponents"]

- name: "BadgeStatus"
  definition: "Status badge variants (active, pending, inactive, error) for enterprise applications."
  anchor_file: "src/scss/custom.scss"
  related: ["CustomComponents"]

- name: "TableEnterprise"
  definition: "Enhanced table styles with dark header, hover states, and compact variant for data-heavy views."
  anchor_file: "src/scss/custom.scss"
  related: ["CustomComponents"]

- name: "AccessibilityFocus"
  definition: "Enhanced focus-visible styles ensuring keyboard navigation meets WCAG AA standards."
  anchor_file: "src/scss/custom.scss"
  related: ["EnterpriseTheme"]

- name: "ErrorDialog"
  definition: "Vanilla TypeScript modal component for displaying literate error messages using Bootstrap 5 Modal API. Supports suggestion boxes, collapsible technical details, copy-to-clipboard, and optional retry."
  anchor_file: "components/errordialog/errordialog.ts"
  related: ["LiterateErrors", "CustomComponents", "ErrorDialogStyles", "BootstrapIcons"]

- name: "ErrorDialogStyles"
  definition: "SCSS styles for the ErrorDialog component with errordialog- prefixed classes."
  anchor_file: "components/errordialog/errordialog.scss"
  related: ["ErrorDialog", "EnterpriseTheme"]

- name: "BootstrapIcons"
  definition: "Bootstrap Icons font package bundled locally from npm to dist/icons/ for UI icons."
  anchor_file: "dist/icons/bootstrap-icons.css"
  related: ["ErrorDialog"]

- name: "LiterateErrors"
  definition: "Human-centric error handling pattern with user-facing and technical facets."
  anchor_file: "LITERATE_ERRORS.md"
  related: ["ErrorDialog"]

- name: "EditableComboBox"
  definition: "Vanilla TypeScript component combining a text input with a filterable dropdown list. Supports free text, substring filtering, keyboard navigation, item grouping, size variants, and WAI-ARIA combobox pattern."
  anchor_file: "components/editablecombobox/editablecombobox.ts"
  related: ["CustomComponents", "EditableComboBoxStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "EditableComboBoxStyles"
  definition: "SCSS styles for the EditableComboBox component with combobox- prefixed classes."
  anchor_file: "components/editablecombobox/editablecombobox.scss"
  related: ["EditableComboBox", "EnterpriseTheme"]

- name: "CloudIcons"
  definition: "SVG icon sets for AWS, Azure, and GCP cloud services."
  anchor_file: "cloud-icons/"
  related: []

- name: "DatePicker"
  definition: "Vanilla TypeScript calendar date picker with day, month, and year navigation views. Supports configurable format, manual input parsing, week numbers, locale-aware first day of week, min/max constraints, and format hint with help tooltip."
  anchor_file: "components/datepicker/datepicker.ts"
  related: ["CustomComponents", "DatePickerStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "DatePickerStyles"
  definition: "SCSS styles for the DatePicker component with datepicker- prefixed classes."
  anchor_file: "components/datepicker/datepicker.scss"
  related: ["DatePicker", "EnterpriseTheme"]

- name: "TimePicker"
  definition: "Vanilla TypeScript time-of-day picker with spinner columns for hours, minutes, and seconds. Supports 12/24-hour modes, optional timezone selector with searchable IANA dropdown, and format hint with help tooltip."
  anchor_file: "components/timepicker/timepicker.ts"
  related: ["CustomComponents", "TimePickerStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "TimePickerStyles"
  definition: "SCSS styles for the TimePicker component with timepicker- prefixed classes."
  anchor_file: "components/timepicker/timepicker.scss"
  related: ["TimePicker", "EnterpriseTheme"]

- name: "DurationPicker"
  definition: "Vanilla TypeScript duration/interval picker with 17 configurable unit patterns, ISO 8601 input/output, carry mode, and 1-year maximum. Supports format hint with help tooltip."
  anchor_file: "components/durationpicker/durationpicker.ts"
  related: ["CustomComponents", "DurationPickerStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "DurationPickerStyles"
  definition: "SCSS styles for the DurationPicker component with durationpicker- prefixed classes."
  anchor_file: "components/durationpicker/durationpicker.scss"
  related: ["DurationPicker", "EnterpriseTheme"]

- name: "ProgressModal"
  definition: "Vanilla TypeScript modal dialog for long-running operations with indeterminate (spinner) and determinate (progress bar) modes, scrollable timestamped detail log, and Cancel/Retry/Close lifecycle management."
  anchor_file: "components/progressmodal/progressmodal.ts"
  related: ["CustomComponents", "ProgressModalStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "ProgressModalStyles"
  definition: "SCSS styles for the ProgressModal component with progressmodal- prefixed classes and keyframe animations."
  anchor_file: "components/progressmodal/progressmodal.scss"
  related: ["ProgressModal", "EnterpriseTheme"]

- name: "TimezonePicker"
  definition: "Vanilla TypeScript searchable dropdown for IANA timezones with grouped regions, UTC offset display, and live current-time preview. Standalone extraction from TimePicker's embedded timezone selector."
  anchor_file: "components/timezonepicker/timezonepicker.ts"
  related: ["CustomComponents", "TimezonePickerStyles", "EnterpriseTheme", "BootstrapIcons", "TimePicker"]

- name: "TimezonePickerStyles"
  definition: "SCSS styles for the TimezonePicker component with timezonepicker- prefixed classes."
  anchor_file: "components/timezonepicker/timezonepicker.scss"
  related: ["TimezonePicker", "EnterpriseTheme"]

- name: "CronPicker"
  definition: "Vanilla TypeScript visual builder for 6-field CRON expressions with presets, mode-based field editors, human-readable descriptions, and bidirectional raw expression editing."
  anchor_file: "components/cronpicker/cronpicker.ts"
  related: ["CustomComponents", "CronPickerStyles", "EnterpriseTheme"]

- name: "CronPickerStyles"
  definition: "SCSS styles for the CronPicker component with cronpicker- prefixed classes."
  anchor_file: "components/cronpicker/cronpicker.scss"
  related: ["CronPicker", "EnterpriseTheme"]

- name: "MarkdownEditor"
  definition: "Bootstrap 5-themed wrapper around Vditor markdown editor with tab/side-by-side layouts, collapsible panes, inline selection toolbar, GFM, Mermaid/Graphviz/PlantUML diagram rendering, DOMPurify sanitisation, export, and modal hosting. Supports mode: 'display' for chrome-free read-only markdown rendering via Vditor.preview(), and mode: 'naked' for a chrome-free editable Vditor surface (no header/toolbar/tabs) that feels like a textarea with markdown powers — CSS resize: both, form-control border."
  anchor_file: "components/markdowneditor/markdowneditor.ts"
  related: ["CustomComponents", "MarkdownEditorStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "MarkdownEditorStyles"
  definition: "SCSS styles for the MarkdownEditor component with mde- prefixed classes, Vditor theme overrides."
  anchor_file: "components/markdowneditor/markdowneditor.scss"
  related: ["MarkdownEditor", "EnterpriseTheme"]

- name: "StatusBar"
  definition: "Vanilla TypeScript fixed-bottom viewport status bar with configurable label/value regions, pipe dividers, O(1) region updates via Map, natively selectable text, and --statusbar-height CSS custom property."
  anchor_file: "components/statusbar/statusbar.ts"
  related: ["CustomComponents", "StatusBarStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "StatusBarStyles"
  definition: "SCSS styles for the StatusBar component with statusbar- prefixed classes and sm/md/lg size variants."
  anchor_file: "components/statusbar/statusbar.scss"
  related: ["StatusBar", "EnterpriseTheme"]

- name: "SidebarPanel"
  definition: "Vanilla TypeScript dockable, floatable, resizable sidebar panel with tab grouping, collapse-to-icon-strip, drag-to-dock, viewport-relative maxWidth default (50% of window.innerWidth), and CSS custom property integration for main content offset. Uses .sidebar-container root class to avoid collision with existing .sidebar nav styles."
  anchor_file: "components/sidebar/sidebar.ts"
  related: ["CustomComponents", "SidebarStyles", "EnterpriseTheme", "BootstrapIcons", "StatusBar"]

- name: "SidebarStyles"
  definition: "SCSS styles for the Sidebar component with sidebar-container root class, mode modifiers, title bar, resize handles, collapsed strip, tab bar, and dock zone drop indicators."
  anchor_file: "components/sidebar/sidebar.scss"
  related: ["SidebarPanel", "EnterpriseTheme"]

- name: "BannerBar"
  definition: "Vanilla TypeScript fixed-to-top viewport banner for announcing significant events. Supports info/warning/critical/success severity presets, optional title/icon/action, auto-dismiss timer, full colour overrides, single-instance model, and --bannerbar-height CSS custom property."
  anchor_file: "components/bannerbar/bannerbar.ts"
  related: ["CustomComponents", "BannerBarStyles", "EnterpriseTheme", "BootstrapIcons", "StatusBar", "SidebarPanel"]

- name: "BannerBarStyles"
  definition: "SCSS styles for the BannerBar component with bannerbar- prefixed classes, variant colour presets, and slide-in animation."
  anchor_file: "components/bannerbar/bannerbar.scss"
  related: ["BannerBar", "EnterpriseTheme"]

- name: "Gauge"
  definition: "Vanilla TypeScript visual measure component modeled after the ASN.1 Gauge type. Supports tile (square), ring (circular SVG), and bar (linear) shapes with value and time modes, configurable colour thresholds, auto-tick countdown, over-limit/overdue states, fluid or explicit sizing, custom formatting, and ARIA meter/timer roles."
  anchor_file: "components/gauge/gauge.ts"
  related: ["CustomComponents", "GaugeStyles", "EnterpriseTheme"]

- name: "GaugeStyles"
  definition: "SCSS styles for the Gauge component with gauge- prefixed classes, tile/ring/bar shape modifiers, size variants (xs-xl + fluid), vertical bar variant, overlimit/overdue state classes, container query fluid typography, and zero animations."
  anchor_file: "components/gauge/gauge.scss"
  related: ["Gauge", "EnterpriseTheme"]

- name: "Conversation"
  definition: "Vanilla TypeScript turn-by-turn AI chat UI with Vditor-rendered assistant messages, streaming token-by-token responses, session management (new/load/clear/save callbacks), thumbs up/down feedback with optional written comments, clipboard copy in Markdown/HTML/plaintext, inline error display with expandable technical details, MCP App rendering in sandboxed iframes with JSON-RPC 2.0 postMessage bridge and theme injection, optional resizable canvas side panel for full-size MCP app display, message buffer eviction, size variants (sm/md/lg), and WAI-ARIA log pattern. Callback-driven — makes no network requests."
  anchor_file: "components/conversation/conversation.ts"
  related: ["CustomComponents", "ConversationStyles", "EnterpriseTheme", "BootstrapIcons", "MarkdownEditor", "McpAppFrame"]

- name: "McpAppFrame"
  definition: "Private inner class managing a single MCP App sandboxed iframe with CSP meta tag injection, Bootstrap theme variable pass-through via getComputedStyle, JSON-RPC 2.0 postMessage bridge (guest-side window.mcpBridge with send/onMessage), event.source validation, unique per-frame appId routing, and cleanup on destroy."
  anchor_file: "components/conversation/conversation.ts"
  related: ["Conversation", "ConversationStyles"]

- name: "ConversationStyles"
  definition: "SCSS styles for the Conversation component with conversation- prefixed classes, role variant backgrounds, Vditor rich content overrides, typing indicator bounce animation, streaming cursor blink animation, feedback modal, copy feedback icon swap, MCP app inline frame, canvas side panel, canvas resize handle, size variants (sm/lg), and container query responsive breakpoints."
  anchor_file: "components/conversation/conversation.scss"
  related: ["Conversation", "EnterpriseTheme"]

- name: "Timeline"
  definition: "Vanilla TypeScript horizontal event timeline component for visualising point events (pins at a moment) and span events (blocks with start-end) on a time axis. Supports greedy row packing, grouping with collapsible collapse-to-presence-band, selection/click callbacks, IntersectionObserver viewport visibility, now marker, size variants (sm/md/lg), container query responsiveness, configurable IANA timezone display with built-in selector, explicit tick interval control, and PointerEvent-based drag-to-pan with RAF throttle. Callback-driven — makes no network requests."
  anchor_file: "components/timeline/timeline.ts"
  related: ["CustomComponents", "TimelineStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "TimelineStyles"
  definition: "SCSS styles for the Timeline component with timeline- prefixed classes, axis ticks, group labels with collapse toggle, span and point item positioning, selection outline, presence band for collapsed groups, now marker, size variants, container queries, focus/keyboard accessibility, pannable cursor states, and timezone selector badge/dropdown."
  anchor_file: "components/timeline/timeline.scss"
  related: ["Timeline", "EnterpriseTheme"]

- name: "TabbedPanel"
  definition: "Vanilla TypeScript dockable, floatable, collapsible, resizable tabbed panel component for grouping related content into tabs. Docks to top/bottom viewport edges, collapses to 32px strip, resizes via pointer-capture drag handles, supports configurable tab bar position (top/left/bottom/right), dynamic tab add/remove, drag-to-dock with drop zones, title resolution (explicit > active tab > 'Panel'), and CSS custom property integration (--tabbedpanel-top-height, --tabbedpanel-bottom-height). Complementary to Sidebar (left/right edges). Uses TabbedPanelManager singleton for multi-panel coordination."
  anchor_file: "components/tabbedpanel/tabbedpanel.ts"
  related: ["CustomComponents", "TabbedPanelStyles", "EnterpriseTheme", "BootstrapIcons", "SidebarPanel", "StatusBar", "BannerBar"]

- name: "TabbedPanelStyles"
  definition: "SCSS styles for the TabbedPanel component with tabbedpanel- prefixed classes, docked/floating/collapsed mode modifiers, tab bar position variants (top/left/bottom/right), tab title modes (icon/text/icon-text), resize handles, collapsed strip, dock zone drop indicators, and custom style overrides."
  anchor_file: "components/tabbedpanel/tabbedpanel.scss"
  related: ["TabbedPanel", "EnterpriseTheme"]

- name: "TreeView"
  definition: "Highly configurable tree view component for multi-tree structured data. Supports lazy loading, multi-select, drag-and-drop (internal + cross-tree + external), context menu, inline rename, search with mark highlighting, starred/favourites group, sort modes, extensible node types with badges, toolbar actions, and WAI-ARIA tree pattern keyboard navigation. Uses treeview- CSS prefix, context menu z-index 1050."
  anchor_file: "components/treeview/treeview.ts"
  related: ["CustomComponents", "TreeViewStyles", "EnterpriseTheme", "BootstrapIcons"]

- name: "TreeViewStyles"
  definition: "SCSS styles for the TreeView component with treeview- prefixed classes, node row states (hover/selected/focused/disabled/loading/dragging), toggle chevron, star toggle, badges, inline rename input, drop indicators (before/inside/after), context menu, starred group, search match highlighting, empty state, and reduced motion."
  anchor_file: "components/treeview/treeview.scss"
  related: ["TreeView", "EnterpriseTheme"]

- name: "TreeGrid"
  definition: "Vanilla TypeScript tree-grid hybrid component combining hierarchical tree in column 1 with tabular data in remaining columns. Supports inline cell editing (text, number, select, date), column resize/reorder/sort, row selection, 2D cell-level keyboard navigation (WAI-ARIA treegrid pattern), virtual scrolling for 1M+ rows, summary/aggregate rows, drag-and-drop, context menu, row striping. Uses FocusCoord {row, col} model, sticky tree column, flexbox rows with JS-managed column widths. Standalone implementation (ADR-021) sharing patterns with TreeView."
  anchor_file: "components/treegrid/treegrid.ts"
  related: ["CustomComponents", "TreeGridStyles", "EnterpriseTheme", "BootstrapIcons", "TreeView"]

- name: "TreeGridStyles"
  definition: "SCSS styles for the TreeGrid component with treegrid- prefixed classes, sticky header row, tree column with toggle chevron, cell types (text/number/select/date), inline edit input, column resize handles, column header sort indicators, row states (selected/focused/hover/striped), cell focus ring, summary/aggregate rows, context menu, drop indicators, and virtual scrolling container."
  anchor_file: "components/treegrid/treegrid.scss"
  related: ["TreeGrid", "EnterpriseTheme"]

- name: "FocusCoord"
  definition: "Two-dimensional cell coordinate model {row: number, col: number} used by TreeGrid for cell-level keyboard navigation. Row is the visible row index, col is the column index (0 = tree column, 1+ = data columns). Used to track focused cell, selection anchor, and active edit cell."
  anchor_file: "components/treegrid/treegrid.ts"
  related: ["TreeGrid"]

- name: "ThemeDemo"
  definition: "Demo page showcasing all enterprise theme components and Bootstrap overrides including ErrorDialog, EditableComboBox, DatePicker, TimePicker, DurationPicker, ProgressModal, TimezonePicker, CronPicker, MarkdownEditor, StatusBar, Sidebar, BannerBar, Toolbar, Gauge, Conversation, Timeline, TabbedPanel, TreeView, and DockLayout."
  anchor_file: "demo/index.html"
  related: ["CustomComponents", "EnterpriseTheme", "MetricCard", "Toolbar", "ToolbarStyles", "Sidebar", "ErrorDialog", "EditableComboBox", "DatePicker", "TimePicker", "DurationPicker", "ProgressModal", "TimezonePicker", "CronPicker", "MarkdownEditor", "StatusBar", "SidebarPanel", "BannerBar", "Gauge", "GaugeStyles", "Conversation", "ConversationStyles", "Timeline", "TimelineStyles", "TabbedPanel", "TabbedPanelStyles", "TreeView", "TreeViewStyles", "DockLayout", "DockLayoutStyles"]

- name: "RepoIndex"
  definition: "SQLite-based full-text search indexer for fast repository navigation by agents."
  anchor_file: "scripts/repo-index.py"
  related: []

- name: "DocumentationSystem"
  definition: "Auto-generated and hand-written documentation system. Markdown sources in docs/ are converted to themed HTML in dist/docs/ during build."
  anchor_file: "scripts/generate-docs.js"
  related: ["DesignTokens", "AgentQuickRef", "EnterpriseTheme"]

- name: "DesignTokens"
  definition: "Auto-generated catalogue of all SCSS variables from _variables.scss, grouped by section with resolved values."
  anchor_file: "docs/DESIGN_TOKENS.md"
  related: ["EnterpriseTheme", "DocumentationSystem"]

- name: "AgentQuickRef"
  definition: "Auto-generated machine-parseable quick reference combining dist paths, design tokens, CSS classes, and component APIs."
  anchor_file: "docs/AGENT_QUICK_REF.md"
  related: ["DocumentationSystem", "DesignTokens", "CustomComponents"]

- name: "DockLayout"
  definition: "Vanilla TypeScript CSS Grid-based layout coordinator arranging Toolbar, Sidebar, TabbedPanel, StatusBar, and content into a 5-zone application shell. Inspired by Java Swing BorderLayout. Auto-sets contained mode on child components. Supports dynamic slot management, resize/collapse listeners, grid template auto-update, and onLayoutChange callback."
  anchor_file: "components/docklayout/docklayout.ts"
  related: ["CustomComponents", "DockLayoutStyles", "EnterpriseTheme", "Toolbar", "SidebarPanel", "TabbedPanel", "StatusBar"]

- name: "DockLayoutStyles"
  definition: "SCSS styles for the DockLayout component with dock-layout- prefixed classes, CSS Grid named areas (toolbar/left/center/right/bottom/status), empty slot collapse rules, and center content fill rule."
  anchor_file: "components/docklayout/docklayout.scss"
  related: ["DockLayout", "EnterpriseTheme"]

- name: "ToolbarRegionAlign"
  definition: "Left/right alignment type for toolbar regions. Regions with align 'right' are pushed to the trailing edge of the toolbar via a flex spacer, while 'left' (default) regions stay at the leading edge. Enables split toolbar layouts with actions on both sides."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles"]

- name: "ToolbarRightContent"
  definition: "Custom HTML slot on the right edge of the toolbar, outside the region/overflow system. Accepts any HTMLElement via rightContent option or setRightContent() API. Used for embedding search bars, user menus, or other custom content that should not participate in overflow."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles"]

- name: "ToolbarInputItem"
  definition: "First-class toolbar item type for embedded text inputs. Supports optional icon, placeholder, onInput and onSubmit callbacks. Integrates into the existing tool tracking and overflow system. Built by buildInputItem(). SCSS class: .toolbar-input-wrap."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles", "ToolbarDropdownItem", "ToolbarLabelItem"]

- name: "ToolbarDropdownItem"
  definition: "First-class toolbar item type for embedded select dropdowns. Supports options array, selected value, and onChange callback. Integrates into the existing tool tracking and overflow system. Built by buildDropdownItem(). SCSS class: .toolbar-dropdown."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles", "ToolbarInputItem", "ToolbarLabelItem"]

- name: "ToolbarLabelItem"
  definition: "First-class toolbar item type for static non-interactive labels. Supports optional icon and colour. Does not participate in user interaction — used for displaying status text, counters, or indicators. Built by buildLabelItem(). SCSS class: .toolbar-label."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles", "ToolbarInputItem", "ToolbarDropdownItem"]

- name: "ToolbarTitleWidth"
  definition: "Fixed width option for the toolbar title/branding area. Set via width property on ToolbarTitle interface. Applied by applyTitleDimensions() helper. setTitle() triggers recalculateOverflow() to account for title width changes affecting available tool space."
  anchor_file: "components/toolbar/toolbar.ts"
  related: ["Toolbar", "ToolbarStyles"]

- name: "LogConsole"
  definition: "Reusable in-app logging console component for high-level user actions and system events. Configurable themes, level colours, fonts, rAF-batched rendering, FIFO eviction."
  anchor_file: "components/logconsole/logconsole.ts"
  related: ["EnterpriseTheme", "TabbedPanel", "StatusBar"]

- name: "MaskedEntry"
  definition: "Masked input for sensitive non-password data (API keys, tokens, connection strings). Show/hide toggle, copy-to-clipboard, two masking strategies (native browser type=password or custom character replacement). Bootstrap input-group layout."
  anchor_file: "components/maskedentry/maskedentry.ts"
  related: ["EnterpriseTheme"]

- name: "MaskedEntryStyles"
  definition: "SCSS styles for the MaskedEntry component. Uses $gray-50/$gray-900 for input text, monospace font for the input field."
  anchor_file: "components/maskedentry/maskedentry.scss"
  related: ["MaskedEntry", "EnterpriseTheme"]

- name: "SplitLayout"
  definition: "Draggable, resizable split pane container dividing space into N panes with pointer-capture drag on dividers. Flexbox-based with horizontal/vertical orientation, pane collapse/expand, nested layout support, keyboard accessibility, localStorage persistence, and dynamic pane add/remove. Uses splitlayout- CSS prefix."
  anchor_file: "components/splitlayout/splitlayout.ts"
  related: ["EnterpriseTheme", "SplitLayoutStyles"]

- name: "SplitLayoutStyles"
  definition: "SCSS styles for the SplitLayout component with splitlayout- prefixed classes. Flex container with orientation modifiers, divider variants (line, dots, handle), collapsed pane, pointer-capture drag, and keyboard focus styles."
  anchor_file: "components/splitlayout/splitlayout.scss"
  related: ["SplitLayout", "EnterpriseTheme"]

- name: "ColorPicker"
  definition: "Canvas-based colour selection with saturation/brightness gradient, vertical hue strip, optional opacity slider, hex/RGB/HSL format tabs, text inputs, preset swatches, and popup/inline modes. All colour math (hex/RGB/HSL/HSV conversions) implemented internally. Uses colorpicker- CSS prefix, popup z-index 1050."
  anchor_file: "components/colorpicker/colorpicker.ts"
  related: ["EnterpriseTheme", "ColorPickerStyles", "BootstrapIcons"]

- name: "ColorPickerStyles"
  definition: "SCSS styles for the ColorPicker component with colorpicker- prefixed classes. Trigger button, panel, canvas area, hue strip, opacity bar with checkerboard, format tabs, input row, swatches grid, and size variants."
  anchor_file: "components/colorpicker/colorpicker.scss"
  related: ["ColorPicker", "EnterpriseTheme"]

- name: "EmptyState"
  definition: "Centered placeholder for empty views with icon/illustration, heading, description, CTA button, and secondary link. Three size variants (sm/md/lg) and compact mode. Uses emptystate- CSS prefix."
  anchor_file: "components/emptystate/emptystate.ts"
  related: ["EnterpriseTheme", "EmptyStateStyles", "BootstrapIcons"]

- name: "EmptyStateStyles"
  definition: "SCSS styles for the EmptyState component with emptystate- prefixed classes. Centered flex column, size variants, compact mode, icon/heading/description/action/secondary styling."
  anchor_file: "components/emptystate/emptystate.scss"
  related: ["EmptyState", "EnterpriseTheme"]

- name: "SkeletonLoader"
  definition: "Animated placeholder mimicking content layout during loading. CSS shimmer animation with six presets (text, avatar, card, table, paragraph, custom). Minimal JS, CSS-heavy. Uses skeleton- CSS prefix."
  anchor_file: "components/skeletonloader/skeletonloader.ts"
  related: ["EnterpriseTheme", "SkeletonLoaderStyles"]

- name: "SkeletonLoaderStyles"
  definition: "SCSS styles for the SkeletonLoader component. Shimmer keyframe animation, skeleton-shimmer-base mixin, preset modifiers, no-animate class, and prefers-reduced-motion override."
  anchor_file: "components/skeletonloader/skeletonloader.scss"
  related: ["SkeletonLoader", "EnterpriseTheme"]

- name: "Toast"
  definition: "Transient non-blocking notification system with singleton container at configurable viewport corners. Stacking with max visible + queue, auto-dismiss with progress bar, pause on hover, four severity variants (info/success/warning/error), action buttons, keyboard Escape dismiss. Z-index 1070 (ADR-032). Uses pvk-toast- CSS prefix to avoid Bootstrap collision."
  anchor_file: "components/toast/toast.ts"
  related: ["EnterpriseTheme", "ToastStyles", "BootstrapIcons"]

- name: "ToastStyles"
  definition: "SCSS styles for the Toast component with pvk-toast- prefixed classes. Fixed container positioning, variant left borders, slide entrance/exit animations, progress bar countdown, hover pause, and reduced motion support."
  anchor_file: "components/toast/toast.scss"
  related: ["Toast", "EnterpriseTheme"]

- name: "MultiselectCombo"
  definition: "Multi-select combo box with chips, checkboxes, filtering, grouping, Select All, and configurable display modes. Supports chip or count badge display, max selections, item grouping with headers, and three size variants. Standalone implementation sharing EditableComboBox design conventions."
  anchor_file: "components/multiselectcombo/multiselectcombo.ts"
  related: ["EnterpriseTheme", "MultiselectComboStyles", "BootstrapIcons"]

- name: "MultiselectComboStyles"
  definition: "SCSS styles for the MultiselectCombo component. Input area with chips and toggle, dropdown panel with filter/select-all/listbox, group headers, item checkboxes, overflow badge, size variants, disabled/readonly states, and reduced motion support."
  anchor_file: "components/multiselectcombo/multiselectcombo.scss"
  related: ["MultiselectCombo", "EnterpriseTheme"]

- name: "DataGrid"
  definition: "High-performance flat data table with sorting, filtering, pagination, column resize, row selection, inline editing, virtual scrolling, footer aggregation, CSV export, dense mode, row striping, pinned columns, and column reorder. Standalone component (ADR-029) using flexbox rows with JS-managed column widths."
  anchor_file: "components/datagrid/datagrid.ts"
  related: ["EnterpriseTheme", "DataGridStyles", "BootstrapIcons"]

- name: "DataGridStyles"
  definition: "SCSS styles for the DataGrid component. Header row, filter row, body rows/cells, footer, pagination bar, resize handles, sort indicators, selection highlighting, inline editor, checkbox/row-number columns, pinned columns, dense mode, striped rows, column reorder drag/drop, and reduced motion support."
  anchor_file: "components/datagrid/datagrid.scss"
  related: ["DataGrid", "EnterpriseTheme"]

- name: "CodeEditor"
  definition: "Bootstrap 5-themed code editor wrapping CodeMirror 6 (ADR-028) with syntax highlighting, toolbar, diagnostics, and textarea fallback. Supports 10 languages, light/dark themes via CSS custom properties, read-only mode, word wrap, auto-grow, copy-to-clipboard, Ctrl+S save, and disabled state."
  anchor_file: "components/codeeditor/codeeditor.ts"
  related: ["EnterpriseTheme", "CodeEditorStyles", "BootstrapIcons"]

- name: "CodeEditorStyles"
  definition: "SCSS styles for the CodeEditor component. Toolbar with buttons and language selector, editor area, fallback textarea, fallback header, diagnostic gutter markers, light and dark themes via CSS custom properties, readonly/disabled states, and reduced motion support."
  anchor_file: "components/codeeditor/codeeditor.scss"
  related: ["CodeEditor", "EnterpriseTheme"]

- name: "Tagger"
  definition: "Combined freeform and controlled-vocabulary tag input. Taxonomy categories with allowed values, category badges, three color modes (category/hash/none), inline autocomplete dropdown with category groups, highlight matching, freeform creation row, per-category and global max tag limits, duplicate handling (reject with flash or ignore), comma-paste splitting, DJB2 hash coloring, custom validation, size variants (sm/default/lg), disabled/readonly states, ARIA combobox/listbox pattern with live region announcements."
  anchor_file: "components/tagger/tagger.ts"
  related: ["EnterpriseTheme", "TaggerStyles", "BootstrapIcons"]

- name: "TaggerStyles"
  definition: "SCSS styles for the Tagger component. Chip container with flex-wrap, chips with category badges and colour borders, inline input, autocomplete dropdown with category groups/headers/items/create row, highlight marks, validation error, size variants (sm/default/lg), disabled/readonly states, chip-in and flash animations, and reduced motion support."
  anchor_file: "components/tagger/tagger.scss"
  related: ["Tagger", "EnterpriseTheme"]

- name: "FacetSearch"
  definition: "Facet-aware search bar with structured key:value query parsing, inline facet chips, context-sensitive autocomplete for facet keys and values, operators (:, !:, >, <, >=, <=), negation (-key:value), quoted values, async value loading via loadValues callback, recent search history, standalone parseFacetQuery function, size variants (sm/default/lg), disabled state, ARIA combobox/listbox pattern with live region announcements."
  anchor_file: "components/facetsearch/facetsearch.ts"
  related: ["EnterpriseTheme", "FacetSearchStyles", "BootstrapIcons"]

- name: "FacetSearchStyles"
  definition: "SCSS styles for the FacetSearch component. Search bar with icon/chips/input/clear button, facet chips with negated variant, autocomplete dropdown with sections/headers/items/hints/loading/empty, spinner animation for async loading, size variants (sm/default/lg), disabled state, reduced motion support."
  anchor_file: "components/facetsearch/facetsearch.scss"
  related: ["FacetSearch", "EnterpriseTheme"]

- name: "FileExplorer"
  definition: "Two-pane file navigation component with folder tree sidebar, breadcrumb navigation, three view modes (grid/list/detail), multi-selection, context menu, inline rename, sortable columns, and callback-driven file operations. Built with inline split pane, pointer-capture divider drag, virtual root for multi-root support, O(1) node lookups via nodeMap/parentMap."
  anchor_file: "components/fileexplorer/fileexplorer.ts"
  related: ["EnterpriseTheme", "FileExplorerStyles", "BootstrapIcons"]

- name: "FileExplorerStyles"
  definition: "SCSS styles for the FileExplorer component. Root container, toolbar with view mode buttons and sort controls, split layout with tree pane and content pane, resizable divider, folder tree items with chevrons and active state, breadcrumbs, grid/list/detail view modes, sortable column headers, rename input, context menu, empty state, status line, drop target indicator, reduced motion support."
  anchor_file: "components/fileexplorer/fileexplorer.scss"
  related: ["FileExplorer", "EnterpriseTheme"]

- name: "CommentOverlay"
  definition: "Transparent overlay system for anchoring comment pins to DOM elements, enabling inline annotation with threaded discussions, @mentions, resolve/unresolve. Pins positioned via getBoundingClientRect with offset from anchor element, SVG connector lines, pointer-capture drag-to-reposition, placement mode with click-to-place, thread popovers with comment cards (avatar, author, timestamp, text), @mention autocomplete with debounced async search, inline edit/delete with confirmation, resolve/unresolve toggle, ResizeObserver + MutationObserver + scroll repositioning, relative timestamp auto-update, JSON export."
  anchor_file: "components/commentoverlay/commentoverlay.ts"
  related: ["EnterpriseTheme", "CommentOverlayStyles", "BootstrapIcons"]

- name: "CommentOverlayStyles"
  definition: "SCSS styles for the CommentOverlay component. Transparent overlay container with pointer-events none (auto on pins/popovers), placement mode crosshair cursor, circular pin buttons with badge/active/resolved/dragging states, SVG connector layer, thread popover dialog with header/body/footer, comment cards with avatar initials and meta, actions overflow menu, reply input with send button, resolve/reopen button, @mention chips and autocomplete dropdown, inline edit textarea with save/cancel, delete confirmation box, reduced motion support."
  anchor_file: "components/commentoverlay/commentoverlay.scss"
  related: ["CommentOverlay", "EnterpriseTheme"]

- name: "ReasoningAccordion"
  definition: "Collapsible accordion for AI chain-of-thought reasoning steps. Four status states (pending/thinking/complete/error) with shimmer animation on thinking headers, spinning icon, per-step timing display, optional confidence progress bars (high/moderate/low colour-coded), size variants (sm/default/lg), auto-expand active steps, auto-collapse completed steps, Vditor markdown rendering with DOMPurify sanitisation, expand-all/collapse-all toggle, summary header with step count and total duration, keyboard navigation (arrows/home/end/enter/space). ~650 lines TypeScript."
  anchor_file: "components/reasoningaccordion/reasoningaccordion.ts"
  related: ["EnterpriseTheme", "ReasoningAccordionStyles", "BootstrapIcons", "Conversation"]

- name: "ReasoningAccordionStyles"
  definition: "SCSS styles for the ReasoningAccordion component. Shimmer keyframe animation on thinking step headers (linear-gradient sweep), spin keyframe for rotating status icons, status-specific colours (gray-400 pending, blue-600 thinking, green-600 complete, red-600 error), confidence bar track with high/moderate/low colour fills, three size variants (sm/default/lg) with scaled padding/fonts/icon sizes, expand/collapse button, step separator borders, error left-border accent, reduced motion media query disabling all animations and transitions."
  anchor_file: "components/reasoningaccordion/reasoningaccordion.scss"
  related: ["ReasoningAccordion", "EnterpriseTheme"]

- name: "CommandPalette"
  definition: "Keyboard-first command palette (Ctrl+K / Cmd+K omnibar) for searching and executing registered commands. Singleton pattern, fuzzy search with prefix/substring/character-skip scoring across label/keywords/description, match highlighting with <mark> elements, category grouping, recent commands persisted to localStorage, configurable maxResults with 'Show all' footer, backdrop overlay at z-index 1080, ARIA dialog/combobox/listbox/option roles, focus trapping, arrow key navigation with wrapping, AbortController for listener cleanup. ~600 lines TypeScript."
  anchor_file: "components/commandpalette/commandpalette.ts"
  related: ["EnterpriseTheme", "CommandPaletteStyles", "BootstrapIcons", "Toolbar"]

- name: "CommandPaletteStyles"
  definition: "SCSS styles for the CommandPalette component. Fixed backdrop with configurable opacity via CSS custom property, centred container with scale+opacity entrance animation, search row with icon and prominent input, category headers with gray-100 background, result items with hover/highlighted state, disabled item opacity, keyboard shortcut badges with monospace font and keycap appearance, match highlight with yellow-100 background and bold weight, empty state centred message, footer with 'Show all' link, reduced motion media query."
  anchor_file: "components/commandpalette/commandpalette.scss"
  related: ["CommandPalette", "EnterpriseTheme"]

- name: "PromptTemplateManager"
  definition: "Two-pane CRUD interface for managing prompt templates with {{variable}} extraction. Inline split layout with draggable divider (pointer-capture), searchable template list with category badges and dirty-state indicators, detail editor with name/description/category/tags, monospace textarea with debounced variable extraction (300ms regex /\\{\\{(\\w+)\\}\\}/g), auto-populated variable editor fields (text/number/select/textarea types), preview with local substitution or async onPreview callback, import/export JSON via FileReader+Blob, status bar with version/save-time/variable-count, keyboard shortcuts (Ctrl+S save, Ctrl+N new, Ctrl+P preview), callback-driven persistence (onSave/onDelete/onDuplicate/onLoadTemplates), read-only mode. ~850 lines TypeScript."
  anchor_file: "components/prompttemplatemanager/prompttemplatemanager.ts"
  related: ["EnterpriseTheme", "PromptTemplateManagerStyles", "BootstrapIcons"]

- name: "PromptTemplateManagerStyles"
  definition: "SCSS styles for the PromptTemplateManager component. Inline split layout with draggable 4px divider (col-resize cursor, blue-600 on hover/active), list pane with search icon and input, list items with 3px left border selection indicator (blue-600) and orange-500 dirty dot, category badges with gray-200 background, detail pane with transparent-border name input, description input, category select, tags container with blue-100 badge pills and inline input, monospace prompt textarea, variable fields with monospace labels, preview panel with gray-100 background and pre-wrap, status bar with pipe separators, read-only mode opacity, reduced motion."
  anchor_file: "components/prompttemplatemanager/prompttemplatemanager.scss"
  related: ["PromptTemplateManager", "EnterpriseTheme"]

- name: "WorkspaceSwitcher"
  definition: "Multi-tenant workspace switcher with trigger button, searchable list in dropdown or modal mode. Deterministic initials avatar colours via DJB2 hash over 8-colour palette, member count, plan badges, size variants (sm/default/lg), debounced search (150ms), optional async onSearch callback, portal-appended dropdown/modal, keyboard navigation (Escape/ArrowUp/ArrowDown/Home/End/Enter). ~530 lines TypeScript."
  anchor_file: "components/workspaceswitcher/workspaceswitcher.ts"
  related: ["EnterpriseTheme", "WorkspaceSwitcherStyles", "BootstrapIcons"]

- name: "WorkspaceSwitcherStyles"
  definition: "SCSS styles for the WorkspaceSwitcher component. Trigger button with chevron and avatar, dropdown panel with position:fixed and box-shadow, modal with fixed inset backdrop, search input, workspace items with avatar/details/check/meta, size variants (sm 28px/default 36px/lg 44px), hover and active states, empty message, reduced motion support."
  anchor_file: "components/workspaceswitcher/workspaceswitcher.scss"
  related: ["WorkspaceSwitcher", "EnterpriseTheme"]

- name: "ActivityFeed"
  definition: "Social-style activity feed with date grouping (Today/Yesterday/This Week/Earlier), IntersectionObserver-based infinite scroll with sentinel element, 7 event types with default icons/colours, deterministic avatar colours, relative timestamps (just now/N minutes ago/yesterday/N days ago), real-time addEvent with entrance animation (translateY + opacity), rich content truncation with Show more/Show less, compact mode with type icons replacing avatars, loading/error/end states for pagination. ~620 lines TypeScript."
  anchor_file: "components/activityfeed/activityfeed.ts"
  related: ["EnterpriseTheme", "ActivityFeedStyles", "BootstrapIcons"]

- name: "ActivityFeedStyles"
  definition: "SCSS styles for the ActivityFeed component. Entrance animation with translateY(-10px) and opacity transition, date group headers with uppercase and divider, event cards with avatar (36px/24px compact)/content/timestamp, avatar initials with colour palette, type icon badges (16px bottom-right), rich content with 3-line clamp, Show more/Show less toggle, compact mode with smaller spacing, loading spinner, empty state, reduced motion support."
  anchor_file: "components/activityfeed/activityfeed.scss"
  related: ["ActivityFeed", "EnterpriseTheme"]

- name: "AuditLogViewer"
  definition: "Read-only filterable audit log viewer with severity badges (info/warning/critical), expandable detail rows with before/after JSON diff, filter bar with date range/actor/action/severity/free-text, removable filter chips, client-side and server-side pagination with page buttons and ellipsis, CSV/JSON export via Blob download, auto-refresh with setInterval, roving tabindex keyboard navigation (ArrowUp/ArrowDown/Home/End/Enter/Space/Escape), fixed column widths with monospace timestamp and IP columns. ~820 lines TypeScript."
  anchor_file: "components/auditlogviewer/auditlogviewer.ts"
  related: ["EnterpriseTheme", "AuditLogViewerStyles", "BootstrapIcons"]

- name: "AuditLogViewerStyles"
  definition: "SCSS styles for the AuditLogViewer component. Flexbox column layout with fixed header/filters/chips and scrollable table body, sticky table header, severity badges with colour-coded backgrounds (gray info/yellow warning/red critical), severity row left-border accents, expandable detail panel with monospace JSON, filter inputs/selects/dates, removable chips, pagination buttons with active state, striped rows, reduced motion support."
  anchor_file: "components/auditlogviewer/auditlogviewer.scss"
  related: ["AuditLogViewer", "EnterpriseTheme"]

- name: "PermissionMatrix"
  definition: "Interactive RBAC permission matrix with CSS Grid layout (roles as columns, grouped permissions as rows). Tri-state checkboxes (granted/denied/inherited/none) with cycle logic, inheritance resolution via resolveChain/resolveEffective with circular detection (max depth 20), bulk group operations, debounced text filter (250ms) with mark highlighting, change tracking with net-zero detection, sticky headers (top:0 z-index:10, left:0 z-index:10, corner z-index:20), group expand/collapse, JSON export via Blob download, arrow key cell navigation, event delegation on grid container. ~880 lines TypeScript."
  anchor_file: "components/permissionmatrix/permissionmatrix.ts"
  related: ["EnterpriseTheme", "PermissionMatrixStyles", "BootstrapIcons"]

- name: "PermissionMatrixStyles"
  definition: "SCSS styles for the PermissionMatrix component. CSS Grid with --pm-role-count custom property, display:contents for header/group/permission rows, sticky headers and first column, checkbox states (green-600 granted, gray-400 denied, blue-500 inherited, dashed-border none), changed cell highlight (yellow-100 70% opacity), inherited cell highlight (blue-100 30% opacity), group chevron rotation transition, toolbar with filter input and action buttons, change counter badge, mark highlight, empty state, reduced motion support."
  anchor_file: "components/permissionmatrix/permissionmatrix.scss"
  related: ["PermissionMatrix", "EnterpriseTheme"]

- name: "GraphToolbar"
  definition: "Factory function that creates a preconfigured Toolbar instance for graph visualization applications (ADR-030). Assembles standard regions: Graph Actions (undo/redo/delete), Layout (dropdown + apply), View (zoom in/out/fit/label, grid snap toggle, minimap toggle), Export (dropdown), Search (input). Returns a GraphToolbarHandle facade with convenience methods for zoom label, toggle states, and button enabled states. Document-level keyboard shortcuts (Ctrl+Z/Y, Delete, Ctrl+=/-, Ctrl+0) with focus/modal guards. ~350 lines TypeScript."
  anchor_file: "components/graphtoolbar/graphtoolbar.ts"
  related: ["EnterpriseTheme", "Toolbar", "GraphToolbarStyles", "BootstrapIcons"]

- name: "GraphToolbarStyles"
  definition: "Minimal SCSS overrides for the GraphToolbar component. Single .graphtoolbar class with zoom label font-weight and min-width adjustments. All core styling provided by Toolbar component SCSS."
  anchor_file: "components/graphtoolbar/graphtoolbar.scss"
  related: ["GraphToolbar", "Toolbar", "EnterpriseTheme"]

- name: "KeyboardShortcutRegistry"
  definition: "Canonical set of keyboard combinations ensuring consistency across all sub-apps and components. Every interactive component exposes an overridable keyBindings option (Partial<Record<string, string>>) with per-action combo strings (e.g. 'Ctrl+Enter'). Defaults in each component's DEFAULT_KEY_BINDINGS constant. Resolution via inlined resolveKeyCombo/matchesKeyCombo helpers."
  anchor_file: "KEYBOARD.md"
  related: ["UX_UI_GUIDELINES", "AccessibilityFocus", "DockLayout", "Conversation"]

- name: "BoxLayout"
  definition: "Single-axis flex layout container. Arranges children sequentially along one axis (horizontal or vertical) with configurable flex factors, alignment, and gap. Per-child flex, minSize, maxSize, alignSelf. ResizeObserver for layout change notifications. ~380 lines TypeScript."
  anchor_file: "components/boxlayout/boxlayout.ts"
  related: ["EnterpriseTheme", "BoxLayoutStyles", "FlowLayout", "LayoutContainers"]

- name: "BoxLayoutStyles"
  definition: "SCSS styles for BoxLayout. display:flex container, .boxlayout-child wrappers with min-width/min-height:0 and overflow:hidden, child fill via > * { width/height: 100% }, reduced motion support."
  anchor_file: "components/boxlayout/boxlayout.scss"
  related: ["BoxLayout", "EnterpriseTheme"]

- name: "FlowLayout"
  definition: "Wrapping flex layout container. Arranges children sequentially and wraps to next line at boundary. Supports separate rowGap/columnGap, alignContent for line distribution, clear() for bulk removal. ~380 lines TypeScript."
  anchor_file: "components/flowlayout/flowlayout.ts"
  related: ["EnterpriseTheme", "FlowLayoutStyles", "BoxLayout", "LayoutContainers"]

- name: "FlowLayoutStyles"
  definition: "SCSS styles for FlowLayout. display:flex with flex-wrap:wrap container, .flowlayout-child wrappers, reduced motion support."
  anchor_file: "components/flowlayout/flowlayout.scss"
  related: ["FlowLayout", "EnterpriseTheme"]

- name: "GridLayout"
  definition: "Uniform CSS Grid layout container. All cells same size. Fixed column count or auto-column mode (recalculates from containerWidth / minCellWidth). Optional aspectRatio for cells. ResizeObserver for auto-column recalculation. ~450 lines TypeScript."
  anchor_file: "components/gridlayout/gridlayout.ts"
  related: ["EnterpriseTheme", "GridLayoutStyles", "FlexGridLayout", "LayoutContainers"]

- name: "GridLayoutStyles"
  definition: "SCSS styles for GridLayout. display:grid container, .gridlayout-cell wrappers with overflow:hidden, child fill, reduced motion support."
  anchor_file: "components/gridlayout/gridlayout.scss"
  related: ["GridLayout", "EnterpriseTheme"]

- name: "BorderLayout"
  definition: "Five-region CSS Grid layout container (North, South, East, West, Center). Grid-template-areas for named region placement. Dynamic grid-template-columns/rows based on slot occupancy. Region collapsing via collapsible option and collapseRegion/expandRegion API. Named setter methods (setNorth, setSouth, etc.). ~400 lines TypeScript."
  anchor_file: "components/borderlayout/borderlayout.ts"
  related: ["EnterpriseTheme", "BorderLayoutStyles", "DockLayout", "LayoutContainers"]

- name: "BorderLayoutStyles"
  definition: "SCSS styles for BorderLayout. CSS Grid with 3x3 template areas (north/west/center/east/south). Per-region grid-area assignments, center content fill, .borderlayout-collapsed state, empty slot collapse, reduced motion support."
  anchor_file: "components/borderlayout/borderlayout.scss"
  related: ["BorderLayout", "EnterpriseTheme"]

- name: "FlexGridLayout"
  definition: "Advanced CSS Grid layout with independently sized rows and columns. Mixed track sizes (px, fr, auto), cell spanning (columnSpan, rowSpan), optional named template areas, per-cell alignment (alignSelf, justifySelf). Cells tracked as array of {config, wrapper}. ~500 lines TypeScript."
  anchor_file: "components/flexgridlayout/flexgridlayout.ts"
  related: ["EnterpriseTheme", "FlexGridLayoutStyles", "GridLayout", "LayoutContainers"]

- name: "FlexGridLayoutStyles"
  definition: "SCSS styles for FlexGridLayout. display:grid container, .flexgridlayout-cell wrappers with overflow:hidden, child fill, reduced motion support."
  anchor_file: "components/flexgridlayout/flexgridlayout.scss"
  related: ["FlexGridLayout", "EnterpriseTheme"]

- name: "CardLayout"
  definition: "Indexed-stack layout container showing one child at a time. Key-based card selection, CSS transitions (fade, slide-left, slide-up) with prefers-reduced-motion respect, lazy loading support, next()/previous() navigation with wrap-around. Transition via CSS animation classes with timeout cleanup. ~420 lines TypeScript."
  anchor_file: "components/cardlayout/cardlayout.ts"
  related: ["EnterpriseTheme", "CardLayoutStyles", "LayerLayout", "LayoutContainers"]

- name: "CardLayoutStyles"
  definition: "SCSS styles for CardLayout. position:relative container, .cardlayout-card wrappers filling 100%, @keyframes for fade-in/out, slide-left in/out, slide-up in/out animations, reduced motion disables all animations."
  anchor_file: "components/cardlayout/cardlayout.scss"
  related: ["CardLayout", "EnterpriseTheme"]

- name: "LayerLayout"
  definition: "Z-stack layout container with all children simultaneously visible. position:relative container, position:absolute layers. 9-point alignment map (top-left through bottom-right) with CSS transform for centering. Anchor offsets (top/right/bottom/left), fill mode, per-layer zIndex. ~350 lines TypeScript."
  anchor_file: "components/layerlayout/layerlayout.ts"
  related: ["EnterpriseTheme", "LayerLayoutStyles", "CardLayout", "AnchorLayout", "LayoutContainers"]

- name: "LayerLayoutStyles"
  definition: "SCSS styles for LayerLayout. position:relative container with overflow:hidden, .layerlayout-layer with position:absolute, reduced motion support."
  anchor_file: "components/layerlayout/layerlayout.scss"
  related: ["LayerLayout", "EnterpriseTheme"]

- name: "AnchorLayout"
  definition: "Constraint-based layout container positioning children via edge anchor relationships. Dual-edge anchoring stretches children, single-edge floats them. anchorCenterH/V for centered positioning with offset. Supports updateAnchors() for dynamic constraint changes, min/max size constraints. ~400 lines TypeScript."
  anchor_file: "components/anchorlayout/anchorlayout.ts"
  related: ["EnterpriseTheme", "AnchorLayoutStyles", "LayerLayout", "LayoutContainers"]

- name: "AnchorLayoutStyles"
  definition: "SCSS styles for AnchorLayout. position:relative container with overflow:hidden, .anchorlayout-child with position:absolute, reduced motion support."
  anchor_file: "components/anchorlayout/anchorlayout.scss"
  related: ["AnchorLayout", "EnterpriseTheme"]

- name: "StatusBadge"
  definition: "Colour-coded status indicator component with dot, pill, and indicator variants for 7 system states (operational, degraded, down, in-progress, failed, maintenance, unknown). Animated pulse for live states. Clickable with onClick callback. Size variants xs/sm/md/lg. ~665 lines TypeScript."
  anchor_file: "components/statusbadge/statusbadge.ts"
  related: ["EnterpriseTheme", "StatusBadgeStyles", "Gauge"]

- name: "StatusBadgeStyles"
  definition: "SCSS styles for StatusBadge. Status colour classes for 7 states, @keyframes pulse animation, dot/pill/indicator variants, size variants, clickable states, prefers-reduced-motion support."
  anchor_file: "components/statusbadge/statusbadge.scss"
  related: ["StatusBadge", "EnterpriseTheme"]

- name: "ConfirmDialog"
  definition: "Promise-based confirmation modal with title, message, icon, variant (default/danger/warning/info), and customisable buttons. showConfirmDialog() returns Promise<boolean>. showDangerConfirmDialog() shortcut for destructive actions. Focus trap, Escape cancels, Enter confirms. ~710 lines TypeScript."
  anchor_file: "components/confirmdialog/confirmdialog.ts"
  related: ["EnterpriseTheme", "ConfirmDialogStyles", "ErrorDialog"]

- name: "ConfirmDialogStyles"
  definition: "SCSS styles for ConfirmDialog. Backdrop fade-in, dialog slide-in animation, variant icon colours, button focus states, prefers-reduced-motion support."
  anchor_file: "components/confirmdialog/confirmdialog.scss"
  related: ["ConfirmDialog", "EnterpriseTheme"]

- name: "SearchBox"
  definition: "Debounced search input with icon, clear button, loading spinner, and optional suggestions dropdown (static array or async function). ARIA combobox pattern, keyboard navigation (ArrowUp/Down, Enter, Escape), live region announcements. Size variants sm/md/lg. ~1256 lines TypeScript."
  anchor_file: "components/searchbox/searchbox.ts"
  related: ["EnterpriseTheme", "SearchBoxStyles", "FacetSearch", "CommandPalette"]

- name: "SearchBoxStyles"
  definition: "SCSS styles for SearchBox. Input wrapper with focus-within state, search icon, clear button, spinner animation, suggestions dropdown, size variants, disabled state, prefers-reduced-motion support."
  anchor_file: "components/searchbox/searchbox.scss"
  related: ["SearchBox", "EnterpriseTheme"]

- name: "UserMenu"
  definition: "Avatar-triggered dropdown menu for user account actions. Displays image or initials avatar with status dot (online/offline/busy/away). Dropdown with menu items, dividers, group headers, danger items. ARIA menu pattern, keyboard navigation. ~963 lines TypeScript."
  anchor_file: "components/usermenu/usermenu.ts"
  related: ["EnterpriseTheme", "UserMenuStyles", "WorkspaceSwitcher"]

- name: "UserMenuStyles"
  definition: "SCSS styles for UserMenu. Trigger button, avatar sizing, status dots, initials colour, dropdown positioning, menu items with hover/focus/danger states, size variants, prefers-reduced-motion support."
  anchor_file: "components/usermenu/usermenu.scss"
  related: ["UserMenu", "EnterpriseTheme"]

- name: "FileUpload"
  definition: "Drag-and-drop file upload zone with progress bars, file validation (type, size, total), retry on failure, and optional download section. File states: queued/uploading/completed/failed. Batch upload support. formatFileSize/getFileIcon helpers. ~1560 lines TypeScript."
  anchor_file: "components/fileupload/fileupload.ts"
  related: ["EnterpriseTheme", "FileUploadStyles", "FileExplorer"]

- name: "FileUploadStyles"
  definition: "SCSS styles for FileUpload. Dashed-border dropzone with active/hover states, file list rows with progress bars, status colours, action buttons, downloads section, size variants, disabled state, prefers-reduced-motion support."
  anchor_file: "components/fileupload/fileupload.scss"
  related: ["FileUpload", "EnterpriseTheme"]

- name: "AppLauncher"
  definition: "Grid-based application launcher with three view modes: dropdown (waffle icon trigger, portal on body), modal (centered overlay with backdrop and category tabs), and fullpage (inline with sidebar navigation). Search with 150ms debounce and optional async fallback, localStorage-persisted favourites and recent apps, badge variants, active app indicator, 2D grid keyboard navigation with roving tabindex. ~950 lines TypeScript."
  anchor_file: "components/applauncher/applauncher.ts"
  related: ["EnterpriseTheme", "AppLauncherStyles", "WorkspaceSwitcher", "CommandPalette", "UserMenu"]

- name: "AppLauncherStyles"
  definition: "SCSS styles for AppLauncher. Trigger button, dropdown portal (fixed, z-1050), modal with backdrop and scale animation (z-1055), fullpage layout with sidebar, CSS Grid tiles with --al-cols custom property, badge variants, favourite star toggle, size variants (sm/default/lg), responsive columns, prefers-reduced-motion support."
  anchor_file: "components/applauncher/applauncher.scss"
  related: ["AppLauncher", "EnterpriseTheme"]

- name: "FormDialog"
  definition: "Modal dialog for form-based workflows with two modes: single-page (scrollable form with collapsible sections, multi-column layout) and wizard (multi-step with numbered step indicator, back/next navigation, per-step validation, configurable slide/fade/none transitions). 12 field types (text, email, password, number, select, textarea, readonly, hidden, checkbox, toggle, date, custom). Resizable sidebar panel with reactive content callback. Built-in required/email/custom validation, focus trapping, loading state with spinner, dirty-change warnings via ConfirmDialog, keyboard shortcuts. ~1500 lines TypeScript."
  anchor_file: "components/formdialog/formdialog.ts"
  related: ["EnterpriseTheme", "FormDialogStyles", "ConfirmDialog", "SplitLayout", "CardLayout"]

- name: "FormDialogStyles"
  definition: "SCSS styles for FormDialog. Overlay (z-2000), backdrop fade, dialog with scale+opacity entrance, four size variants (sm 400px, md 550px, lg 750px, xl 960px), header/title/close, step indicator with numbered circles and connectors, body split mode for panel, collapsible sections with chevron, field rows (half/third), all input/select/textarea styles, panel with drag divider, footer, CSS spinner, wizard page transitions (slide-left/right, fade), prefers-reduced-motion support."
  anchor_file: "components/formdialog/formdialog.scss"
  related: ["FormDialog", "EnterpriseTheme"]

- name: "Ribbon"
  definition: "Microsoft Office-style tabbed toolbar with adaptive group scaling (large/medium/small/collapsed tiers), Quick Access Toolbar (QAT), integrated menu bar with Alt-key activation, full-screen backstage overlay, keyboard-driven KeyTips, 13 built-in control types (button, toggle, split, dropdown, gallery, checkbox, spinner, combobox, color-picker, group, separator, label, custom), contextual tabs, configurable accent colours, layout persistence, and WAI-ARIA toolbar pattern. ~3500 lines TypeScript."
  anchor_file: "components/ribbon/ribbon.ts"
  related: ["EnterpriseTheme", "RibbonStyles", "Toolbar", "BootstrapIcons"]

- name: "RibbonStyles"
  definition: "SCSS styles for the Ribbon component with ribbon- prefixed classes. Tab bar, adaptive group scaling tiers, QAT bar, menu bar with hover/active dropdowns, backstage overlay with sidebar navigation, KeyTip badges, 13 control type styles, gallery popup grid, contextual tab accent colours, collapsed group popup, size variants, and prefers-reduced-motion support."
  anchor_file: "components/ribbon/ribbon.scss"
  related: ["Ribbon", "EnterpriseTheme"]

- name: "FontDropdown"
  definition: "Dropdown picker that renders each font name in its own typeface, similar to Google Docs font selector. 17 built-in web-safe fonts, search filtering with match highlighting, recently-used section via localStorage, keyboard navigation (arrow keys, Enter, Escape), position:fixed dropdown for overflow-safe rendering inside containers with overflow:hidden (e.g. Ribbon panels), sm/default/lg size variants, and prefers-reduced-motion support. ~530 lines TypeScript."
  anchor_file: "components/fontdropdown/fontdropdown.ts"
  related: ["EnterpriseTheme", "EditableComboBox", "SymbolPicker", "Ribbon"]

- name: "FontDropdownStyles"
  definition: "SCSS styles for the FontDropdown component with fontdropdown- prefixed classes. Trigger with caret, dropdown panel, search input, font item list with inline font-family rendering, group headers, separators, match highlights, size variants, disabled state, and prefers-reduced-motion support."
  anchor_file: "components/fontdropdown/fontdropdown.scss"
  related: ["FontDropdown", "EnterpriseTheme"]

- name: "SymbolPicker"
  definition: "Grid-based symbol and icon picker for inserting Unicode characters and Bootstrap Icons. ~500 Unicode symbols in 8 categories (Latin Extended, Greek, Math, Arrows, Currency, Punctuation, Box Drawing, Emoji), ~200 Bootstrap Icons in 10 categories. Category tab bar, search filtering, enlarged preview panel, recently-used section via localStorage, inline/popup modes, configurable grid columns/cell size, Insert button, keyboard navigation, and prefers-reduced-motion support. ~1986 lines TypeScript."
  anchor_file: "components/symbolpicker/symbolpicker.ts"
  related: ["EnterpriseTheme", "ColorPicker", "FontDropdown"]

- name: "SymbolPickerStyles"
  definition: "SCSS styles for the SymbolPicker component with symbolpicker- prefixed classes. Trigger button, panel, mode tabs (Unicode/Icons), search input with icon, scrollable category tab bar, CSS grid for symbol cells, cell hover/highlight/selected states, preview panel with enlarged char/name/code, footer with Insert button, size variants (sm/lg), disabled state, and prefers-reduced-motion support."
  anchor_file: "components/symbolpicker/symbolpicker.scss"
  related: ["SymbolPicker", "EnterpriseTheme"]

- name: "Pill"
  definition: "Reusable inline pill element for mentions, issues, documents, and tags. 6 colour presets, 3 sizes, dismissible/clickable modes, CSS custom properties for runtime theming, anchor mode. Rounded border-radius (9999px) is an explicit exception to the zero-radius theme (ADR-034)."
  anchor_file: "components/pill/pill.ts"
  related: ["SmartTextInput", "Tagger", "StatusBadge"]

- name: "PillStyles"
  definition: "SCSS styles for Pill component with pill- prefixed classes. CSS custom properties (--pill-bg/fg/border-color/border-radius/padding/font-size/max-width), 6 colour preset classes, 3 size variants, dismiss button, clickable state, entrance animation, reduced-motion support."
  anchor_file: "components/pill/pill.scss"
  related: ["Pill", "SmartTextInputStyles", "EnterpriseTheme"]

- name: "SmartTextInput"
  definition: "Behavioral middleware engine that attaches to text inputs and provides trigger-based inline references via state machine detection, popover coordination events, token model, serialization, and two built-in adapters (PlainText, ContentEditable). Non-UI — host provides popover rendering and data sources."
  anchor_file: "components/smarttextinput/smarttextinput.ts"
  related: ["SearchBox", "EditableComboBox", "Tagger", "CommentOverlay", "Conversation", "Pill"]

- name: "SmartTextInputStyles"
  definition: "SCSS styles for STIE token rendering — pill (blue), link (underlined), computed (green monospace), inline-text (italic), with insertion animation, size variants, disabled state, and reduced-motion support."
  anchor_file: "components/smarttextinput/smarttextinput.scss"
  related: ["SmartTextInput", "TaggerStyles", "PillStyles"]

- name: "RichTextInput"
  definition: "Lightweight contenteditable-based rich text input composing STIE and Pill for per-row editing. Inline formatting (bold/italic/strike/code/link), lists (ordered/unordered/task), paste sanitization, floating toolbar on selection, shared selectionchange singleton, HTML-primary serialization with optional markdown output. Zero external dependencies; 20-50 instances per page."
  anchor_file: "components/richtextinput/richtextinput.ts"
  related: ["SmartTextInput", "Pill", "MarkdownEditor"]

- name: "RichTextInputStyles"
  definition: "SCSS styles for RichTextInput with richtextinput- prefixed classes. CSS custom properties (--rti-bg/border-color/focus-border-color/focus-shadow/color/placeholder-color/toolbar-bg/toolbar-border), form-control matching, inline toolbar, task list checkboxes, size variants (sm/lg), disabled/readonly states, resizable, reduced-motion support."
  anchor_file: "components/richtextinput/richtextinput.scss"
  related: ["RichTextInput", "EnterpriseTheme", "PillStyles"]

- name: "PersonChip"
  definition: "Compact inline person-identity chip showing avatar (image or deterministic initials), name, status dot, and optional detail text. Visual style matches UserMenu trigger — transparent background, zero border-radius, hover reveals subtle gray. Three sizes (sm/md/lg), clickable mode with keyboard support, metadata for PeoplePicker integration."
  anchor_file: "components/personchip/personchip.ts"
  related: ["UserMenu", "Pill", "EnterpriseTheme"]

- name: "PersonChipStyles"
  definition: "SCSS styles for PersonChip with personchip- prefixed classes. Matches UserMenu trigger styling — transparent bg, 1px transparent border, hover $gray-100/$gray-300. Avatar sizes (20/28/36px), circular initials with deterministic colours, status dots (online/busy/away/offline), detail text visible at lg only, reduced-motion support."
  anchor_file: "components/personchip/personchip.scss"
  related: ["PersonChip", "UserMenu", "EnterpriseTheme"]

- name: "PeoplePicker"
  definition: "Searchable person selector for share dialogs, assignment fields, permission lists. Frequent contacts dropdown, async search via callback or URL, PersonChip integration for rows (md) and chips (sm), single-select and multi-select modes, overflow badge, keyboard navigation, disabled/readonly states, size variants."
  anchor_file: "components/peoplepicker/peoplepicker.ts"
  related: ["PersonChip", "Tagger", "UserMenu", "SearchBox"]

- name: "PeoplePickerStyles"
  definition: "SCSS styles for PeoplePicker with peoplepicker- prefixed classes. Input area matches form-control, section headers match UserMenu group-header, dropdown z-1050 with box-shadow, row hover/highlight/selected states, spinner animation, size variants (sm/md/lg), disabled/readonly states, reduced-motion support."
  anchor_file: "components/peoplepicker/peoplepicker.scss"
  related: ["PeoplePicker", "PersonChip", "EnterpriseTheme"]

- name: "PresenceIndicator"
  definition: "Compact overlapping avatar stack showing who is actively viewing/editing a shared resource. Composes PersonChip in avatarOnly mode (collapsed) and full PersonChip (expanded). Collapsed stack with z-index ordering, white ring borders, negative-margin overlap, overflow badge. Three size variants (sm 24px, md 32px, lg 40px). Keyboard toggle (Enter/Space/Escape). Runtime PersonChip bridge with inlined fallback."
  anchor_file: "components/presenceindicator/presenceindicator.ts"
  related: ["PersonChip", "PeoplePicker", "EnterpriseTheme"]

- name: "PresenceIndicatorStyles"
  definition: "SCSS styles for PresenceIndicator with presence-indicator- prefixed classes. Collapsed stack with overlapping avatars via negative margins, white ring borders, overflow badge, expanded list, size variants (sm/md/lg), disabled state, focus-visible outline, opacity transition, reduced-motion support."
  anchor_file: "components/presenceindicator/presenceindicator.scss"
  related: ["PresenceIndicator", "PersonChip", "EnterpriseTheme"]

- name: "ShareDialog"
  definition: "Modal share dialog composing PeoplePicker and PersonChip for person selection with configurable access levels. Promise-based API returns diff of added/changed/removed access. FormDialog overlay pattern (z-index 2000) with ConfirmDialog simplicity. Native select for access levels. Three size variants (sm/md/lg). Async onShare callback with loading state. Focus trap and screen reader announcements."
  anchor_file: "components/sharedialog/sharedialog.ts"
  related: ["PeoplePicker", "PersonChip", "FormDialog", "ConfirmDialog"]

- name: "ShareDialogStyles"
  definition: "SCSS styles for ShareDialog with sharedialog- prefixed classes. Overlay fixed z-2000, backdrop fade, dialog scale+fade enter animation, header with title and close, body with add section and existing access list, picker row with flex gap, native form-select access dropdowns, scrollable access list max-height 240px, person fallback with inlined initials, footer with status and action buttons, loading state, reduced-motion support."
  anchor_file: "components/sharedialog/sharedialog.scss"
  related: ["ShareDialog", "FormDialog", "EnterpriseTheme"]

- name: "SpineMap"
  definition: "Interactive SVG capability/feature map with a central spine, branching sub-nodes, four layout algorithms (vertical, horizontal, radial, winding/serpentine), zoom/pan, status color coding, cross-branch connections (depends-on, works-with, blocks, enhances), and three editing modes (sidebar TreeGrid, popover, visual drag). Export SVG/PNG/JSON. Runtime TreeGrid bridge with inlined fallback tree. ~1700 lines TS."
  anchor_file: "components/spinemap/spinemap.ts"
  related: ["TreeGrid", "EnterpriseTheme"]

- name: "SpineMapStyles"
  definition: "SCSS styles for SpineMap with spinemap- prefixed classes. Toolbar, SVG canvas with grab cursor, hub/leaf node hover and focus-visible, selected state, connection hover, popover with edit form, sidebar with TreeGrid slot, legend overlay, fallback tree, size variants (sm/md/lg), live region for accessibility, reduced-motion support."
  anchor_file: "components/spinemap/spinemap.scss"
  related: ["SpineMap", "EnterpriseTheme"]
